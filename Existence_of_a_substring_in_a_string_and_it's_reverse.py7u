class Solution(object):
    def isSubstringPresent(self, s):
        l=[]
        for i in range(len(s)-1):
            l.append(s[i:i+2])
        a=s[::-1]
        for i in l:
            if i in a:
                return True
        else:
            return False
        
                
        
